package com.hour;

import javafx.application.Application;

public class Launch {
    // 使用 jar 包时把此处设为主类 ，从这里打开项目， 跳过模块检查
    public static void main(String[] args) {
        Application.launch(App.class);
    }
}
